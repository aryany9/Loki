## Tasks

- [ ] 1. **Engine wiring (`core:llm`)**: In `LiteRtLlmEngine`, apply `AgentConfig.generationConfig` to inference — build a native `SamplerConfig` (topK/topP/temperature/seed) and pass `maxTokens` through `generate()` to `sendMessageAsync`. Add a **force re-init** path (e.g. `initializeAsync(runtimeConfig, force = true)`) so a loaded engine re-applies backend/KV changes. Add/adjust `LlmEngineTest` coverage.
- [ ] 2. **AgentConfigRepository (`core:ui`)**: Add a DataStore Preferences-backed repository to persist/load an `AgentConfig` (global/default for v1, schema open to a future `modelId` key). Expose a `Flow<AgentConfig>`.
- [ ] 3. **ConversationManager threading (`core:conversation`)**: Make `ConversationManager` hold an active `AgentConfig`, pass it to `newChatSession()`/`newVoiceSession()`, and add `applyAgentConfig(agentConfig)` that (a) on runtime-config change force-release + re-init the engine then `startConversation(agentConfig)`; (b) on generation/system-prompt-only change re-runs `startConversation(agentConfig)`; and (c) resets the active session (mid-conversation save resets context with a confirmation). Load the persisted config at app start (via `MainActivity`/DI).
- [ ] 4. **ConversationSession config (`core:conversation`)**: Accept an `AgentConfig`; merge `systemInstruction` with the tool prompt in `buildSystemPrompt()`; call `startConversation(agentConfig)` (full overload); pass `maxOutputTokens`/generation config through `generate()` instead of the 256 default. Add `ConversationManagerTest`/`ConversationSessionTest` coverage for config threading.
- [ ] 5. Create `AgentPlaygroundViewModel.kt` in `core:ui` to manage `AgentConfig` state, system prompt editing, response-behavior preset mapping (with **preset → "Custom"** label migration when an Advanced control is edited manually), Advanced control editing, validation, save (with **mid-conversation reset confirmation** when a session is active), reset, and test prompt execution (via `ConversationSession.processUtterance()`).
- [ ] 6. Create `AgentPlaygroundScreen.kt` Compose UI in `core:ui` with progressive disclosure: Model info, System Prompt, Response Behavior preset, Test Prompt runner, and a collapsible **Advanced** section (Temperature, Top-K, Top-P, Seed, Max Output Tokens, Backend, Context/KV capacity).
- [ ] 7. **DI (`app`)**: Provide `AgentConfigRepository` in `AppModule.kt`; wire it into `ConversationManager` and the `AgentPlaygroundViewModel`. Add a DataStore delegate in `LokiApplication.kt` if needed.
- [ ] 8. **Navigation & entry points (`app`, `core:ui`)**: Add `AppScreen.AGENT_PLAYGROUND` + routing branch in `MainActivity.kt`; add a top-bar entry point in `ChatScreen.kt` and a Playground entry point in `SetupScreen.kt`.
- [ ] 9. **Tests**: Unit tests for `AgentPlaygroundViewModel` (validation, preset mapping, save/reset, test prompt) and `AgentConfigRepository` (persist/load round-trip).
